<%= form_with(model: attachment, local: true) do |form| %>
  <% if attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
      <% attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :kind %>
    <%= form.number_field :kind, id: :attachment_kind %>
  </div>

  <div class="field">
    <%= form.label :file %>
    <%= form.file_field :file, id: :attachment_file %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
